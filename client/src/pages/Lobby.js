import React from 'react';

const Lobby = () => {
    return (
        <div>
            {/* { Game code } */}
            {/* { Lobby Window} */}
            {/* { Start Button } */}
        </div>
    );
};

export default Lobby;